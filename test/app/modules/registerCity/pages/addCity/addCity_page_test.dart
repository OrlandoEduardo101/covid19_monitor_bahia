import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:covid19monitorbahia/app/modules/registerCity/pages/addCity/addCity_page.dart';

main() {
  testWidgets('AddCityPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddCityPage(title: 'AddCity')));
    //  final titleFinder = find.text('AddCity');
    //  expect(titleFinder, findsOneWidget);
  });
}
